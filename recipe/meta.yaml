{% set name = "starfile" %}
{% set version = "0.5.5" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/starfile-{{ version }}.tar.gz
  sha256: e472c24554a179e74ab2c0a6c43c21a185f081b9d1c3477b009ef9ba4461fd97

build:
  entry_points:
    - starfile = starfile.__main__:cli
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.8
    - setuptools-scm
    - python-build
  run:
    - numpy
    - pandas
    - python >=3.8
    - typing-extensions

test:
  imports:
    - starfile
  commands:
    - pip list
    - pip check
    - starfile --help
  requires:
    - pip

about:
  home: https://pypi.org/project/starfile
  summary: STAR file reading and writing in python.
  license: BSD-3-Clause
  license_file: LICENSE
  dev_url: https://github.com/alisterburt/starfile

extra:
  recipe-maintainers:
    - alisterburt
    - goanpeca
